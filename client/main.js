Meteor.startup(function(){
  var player;


  /*
  if(Session.get("player_id") === undefined){
    player = collections.Players.findOne();

    if(player === undefined){
      Session.set("player_id", collections.Players.insert({}));
    }else{
      Session.set("player_id", player._id);
    }
  }
  */
});
